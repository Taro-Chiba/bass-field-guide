# バス釣り現場ガイド 仕様書(v1.2.0時点)

単一HTML(`bass-guide.html` = `index.html`)のPWA風Webアプリ。フレームワーク不使用、サーバーサイドなし。

- 本番URL: https://taro-chiba.github.io/bass-field-guide/ (GitHub Pages)
- リポジトリ: https://github.com/Taro-Chiba/bass-field-guide (main直デプロイ)
- 予備: Claude Artifact版(CSP制限により天気・地図・Claude相談は不可)
- バージョンタグ: v1.0.0(気象自動取得まで) / v1.1.0(GPSポイントサーチ) / v1.2.0(テストモード・地形解析・Claude相談)
- デプロイ手順: `bass-guide.html` 編集 → `index.html` にコピー → commit & push(数十秒で反映)

---

## 1. 画面構成

2タブ + 画面下固定の実行ボタン「この状況でガイドを見る」。

### ガイドタブ
| カード | 内容 |
|---|---|
| フィールド | フィールド選択チップ / スタイル(オカッパリ・ボート、前回値を記憶) / 「その他」選択時は汎用タイプ(野池・湖ダム・川) / 現地気象バー(自動取得結果+再取得ボタン) |
| ポイントサーチ | 半径チップ(100/300/500m) / テストモード時はLeaflet地図 / 「現在地から有力ポイントを探す」 / ポイントリスト / 「今いる場所をマイポイント登録」 |
| 状況 | 月(1-12) / 時間帯(朝まずめ・日中・夕まずめ・夜) / 天気(快晴・晴れ・曇り・雨・雨後増水) / 風(無風・そよ風・強風) / 水温入力(空欄なら自動推定) / 水質(クリア・ステイン・マッディ) / 目の前のポイント(12種) / 狙う水深(シャロー・ミドル・ディープ・不明) / Claudeへの補足情報(自由入力) |
| 結果 | 戦略メモ → **Claudeの見解カード(主役)** → 「参考: 定石ローテーション」(ルールベース3枚、折りたたみ。キー未設定・Claude失敗時は自動展開されるオフライン保険) → 状況コピーボタン |

### 管理タブ(旧タックルタブ。タックル登録機能はv1.3で廃止)
| カード | 内容 |
|---|---|
| フィールド管理 | 一覧・削除 / 追加(名前・タイプ・緯度経度、📍で現在地取り込み。座標なし=天気オフ) |
| マイポイント | 一覧(種別・水深・登録日)・削除 |
| 設定 | テストモードON/OFF / Claude APIキー・モデル選択・接続テスト |

---

## 2. 外部データ源

| API | 用途 | 認証 | 呼び出しタイミング |
|---|---|---|---|
| Open-Meteo | 現地気象(気温・降水・雲量・風速m/s、過去48h気温/降水、日の出日の入り) | 不要 | 起動時・フィールド切替時・再取得ボタン |
| Overpass API(4ミラーfallback: overpass-api.de → maps.mail.ru → private.coffee → kumi.systems) | OSM地物検索・湖面ポリゴン・流入河川 | 不要 | ポイントサーチ時(キャッシュミス時のみ) |
| OSMタイル + Leaflet 1.9.4(CDN) | テストモードの地図 | 不要 | テストモードON時のみ遅延ロード |
| Claude API(/v1/messages、ブラウザ直接) | 動的ガイド | ユーザー自身のAPIキー | ガイド実行時・追加質問時 |

## 3. 保存データ(すべて端末内localStorage)

| キー | 内容 | 期限 |
|---|---|---|
| bfg_fields / bfg_lastfield | フィールド一覧(プリセット座標はロード時に最新へ上書き)・最終選択 | 無期限 |
| bfg_style | オカッパリ/ボートの前回選択 | 無期限 |
| bfg_mypoints | マイポイント(座標+種別+水深+日付) | 無期限 |
| bfg_clar | フィールドごとの前回水質 | 無期限 |
| bfg_testmode | テストモード状態 | 無期限 |
| bfg_osm_{lat}_{lon}_{r} | OSM地物検索結果(座標3桁グリッド単位) | 30日 |
| bfg_terrain_{fieldId} | 地形解析結果(岬・ワンド・河口) | 90日 |
| bfg_apikey / bfg_aimodel | Claude APIキー・モデル選択 | 無期限 |

プリセットフィールド: 津久井湖・相模湖・亀山湖・高滝湖・三島湖(OSM湖面中心座標)。

---

## 4. 自動入力ロジック(気象→チップ)

- **月・時間帯**: 端末時計。時間帯は日の出日の入り(Open-Meteo)基準 — 朝まずめ=日の出-1h〜+2.5h、夕まずめ=日の入り-2.5h〜+0.5h、夜=それ以外の暗い時間。取得失敗時は固定時刻(4-8/16-19.5)で代替
- **天気**: 現在降水>0.2mm or 降水系weather_code→雨 / 過去24h降水≥8mm→雨後 / 雲量<15%→快晴、<65%→晴れ、以上→曇り
- **風**: <1.5m/s無風、<5.5そよ風、以上強風
- **推定水温**: 0.6×過去48h平均気温 + 0.4×月別基準水温(1-33℃にクランプ)。実測入力が優先
- 自動入力後もすべてのチップは手動で上書き可能

## 5. ルアー推奨エンジン(ルールベース)

1. **ベーススコア**: 21ルアー分類 × 水温6帯(<8 / 8-12 / 12-16 / 16-20 / 20-26 / 26+)の適性表(0-9)
2. **加減点**: 秋(9-11月)巻き物+2 / 時間帯 / 天気 / 風 / 水質 / 水深 / ポイント種別(12種それぞれ固有) / フィールドタイプ
3. **分散選出**: 全21分類から、トップ・巻き・撃ち・フィネスのグループが重複しないよう上位3件を**分類名で**推奨(適合度=最高スコア比%)。手持ち絞り込みはv1.3で廃止
4. **文言生成**: どこに(ポイント種別の定型文。スタイルにより岸起点/船位起点の2系統) / レンジ(分類別、水深分岐) / 動かし方(分類別、低水温<12℃で低速化) / カラー(水質×光量: クリア晴れ=ナチュラル、ロー光量=アピール、マッディ夜=ダーク) / 推奨ロッド(分類→汎用スペック文言) / 理由(グループ別)

## 6. GPSポイントサーチ

1. 位置取得: GPS(高精度・12s timeout)、テストモード時は地図タップの疑似現在地
2. 5km以内に最寄り登録フィールドがあれば自動切替(天気も更新)
3. **地形ポイント**(地形解析キャッシュから距離フィルタ): 岬⛰️・ワンド🌙・河口💧
4. **OSM地物**(半径内Overpass検索): 流れ込み・橋・桟橋/突堤・アシ湿地・ブッシュ・岩盤・ダムサイト・オーバーハング並木・スロープ
5. **マイポイント**(半径内、常に先頭表示)
6. マージ・重複排除(流れ込み系は60m以内で河口優先)、距離+8方位表示、タップでそのポイント条件のガイド実行(マイポイントは水深も適用)
7. テストモード時は検出ポイントを地図に色分けマーカー表示(緑=地形、青=地物、橙=マイ)

### 地形解析(初回のみ・フィールドごと)
- 湖面ポリゴン(natural=water way/relation)取得 → 外周リング組み立て → フィールド中心を含むリングを湖岸線と判定
- 岸沿い80m窓で屈曲評価: 蛇行度≥1.4 or 偏差≥30mを候補、弦中点の水陸判定(point-in-polygon)で岬/ワンド分類、120m間隔NMS、最大150点
- 湖bbox内の全水路を取得し、岸線45m以内に接近する点=河口(60m重複排除、沢名があれば「◯◯の河口」)
- 実測: 津久井湖で65点(岬32・ワンド26・河口7)

## 7. Claude相談(動的ガイド)

- ガイド実行時、ルールベース結果表示と同時に非同期でClaude APIへ問い合わせ
- **送信内容**: システムプロンプト(日本のプロバスガイド・リグ/分類+ウェイトで具体指示・タックル目安付記・400字目安・アプリ推奨への賛否・スタイル別の前提・周辺ポイントがあれば移動先も提案) + 状況サマリー(フィールド/スタイル/日時/時間帯/天気/風/水温/気温/風速/24h降水/水質/ポイント[タップ時は距離・方角付き]/水深) + 現在地の周辺ポイント一覧(直近スキャンがある場合のみ。種別・情報源・距離・方角、最大10件、スキャン経過分数付き) + ルールベース推奨3件(分類名) + 補足情報(任意入力)
- **会話継続**: 見解カード下の入力欄から追加質問(履歴保持、セッション内のみ)
- **モデル**: claude-opus-5(既定) / claude-sonnet-5 / claude-haiku-4-5。opus/sonnetはeffort=medium、max_tokens=1500
- **プロンプト版**: v2(既定。🎯ハードルアー案→🎣操作→🐛ソフトベイト案→🎣操作→💡根拠(共通の読み+どちらから投げるか)→🔄次の一手(もう一方への切替 or 移動先)の2案構成固定、数値断定の強制、状況復唱禁止・「読み」要求、手本1件埋め込み、全体500字目安) / v1(従来・比較用)。設定で切替、bfg_aiprompt に保存
- **認証**: ユーザー自身のAPIキー(設定画面から入力、localStorageのみ、`anthropic-dangerous-direct-browser-access`ヘッダで直接呼び出し)。端末ごとに設定が必要
- キー未登録時は案内表示。「状況をコピー」ボタン(Claudeアプリ相談用文面)は常に利用可能

---

## 8. 既知の制限・積み残し

- **タックル登録機能は廃止(v1.3)**: 登録の手間が大きすぎたため削除。推奨はルアー分類ベース、Claudeは一般的な定番から提案(旧bfg_lures/bfg_rods/bfg_customはロード時に自動削除)
- OSM地物の密度依存: アシ・沈み物・岸際ブッシュは地図に存在しない → マイポイント蓄積で補う設計
- Overpassサーバーは夕方〜夜に混雑(504)しやすい → ミラー+キャッシュで緩和済みだが初回取得が失敗することはある
- オフラインは部分対応: キャッシュ済みの地形・地物とルールベース推奨は動作、天気・新規検索・Claude相談は不可
- Claude相談のプロンプトは未チューニング(実地フィードバック待ち)
- 釣果ログ機能なし(推奨エンジンは実釣データで補正されない)
- 本番モードでの「地図で位置を手動選択」は未実装(テストモードの地図UIを転用可能)
